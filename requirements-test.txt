-r requirements-dev.txt

# Home Assistant Core compatible test dependencies
# pytest-homeassistant-custom-component provides ALL pytest plugins and dependencies:
# - pytest
# - pytest-asyncio
# - pytest-cov
# - pytest-timeout
# - pytest-xdist
# Do NOT explicitly list these - it causes version conflicts!

pytest-homeassistant-custom-component==0.13.306

# Test utilities
aresponses>=2.1.6
freezegun>=1.2.2
zeroconf

# Integration runtime dependencies (mirror manifest.json) so that tests
# can import custom_components.linkplay.media_player.
async-upnp-client>=0.27
validators~=0.12
chardet>=4.0.0

# Code quality
flake8>=6.0.0
ruff>=0.4.2
diff-cover>=9.0.0
