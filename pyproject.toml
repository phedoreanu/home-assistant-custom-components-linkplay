[tool.ruff]
line-length = 120
target-version = "py313"

[tool.ruff.lint]
# F (pyflakes) + targeted E/W rules.  E501 (line-length) is still
# pre-existing in legacy media_player.py blocks and not in scope.
select = ["F", "E711", "E712", "E713", "E722"]
