# Development dependencies
black>=23.0.0
mypy>=1.5.0
pre-commit>=3.0.0

# Note: homeassistant is NOT included here to avoid dependency conflicts.
# Use pytest-homeassistant-custom-component (see requirements-test.txt) for tests.
# Runtime deps live in manifest.json — do not duplicate here.
# ruff lives in requirements-test.txt with the active pin.
